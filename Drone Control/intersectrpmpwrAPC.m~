function [rpm, pwr] = intersectrpmpwrAPC(pwr)
RPMFIT = load('APC_FIT.mat', 'APC_FIT');
APC = load('EXP _DATA_PROP.mat', 'APC');

rpmlim = [3000 12000];

testrpm = linspace(rpmlim(1), rpmlim(2), 10000);
testpwr = RPMFIT.rpm_pwr(testrpm);

L1 = [testrpm; testpwr];
L2 = [rpmlim(1) rpmlim(2); 
